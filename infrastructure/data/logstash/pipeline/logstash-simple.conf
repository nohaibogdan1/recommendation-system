input {
    rabbitmq {
        host => "rabbitmq"
        queue => "logstash_queue"
    }
}

output {
   elasticsearch {
     hosts => "https://elasticsearch"
     ssl_verification_mode => "none"
     ssl_enabled => false
     user => "elastic"
     password => "12345"
     index => "hello"
   }
}