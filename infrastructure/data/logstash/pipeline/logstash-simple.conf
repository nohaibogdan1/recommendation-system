input {
    http {
        host => "0.0.0.0"
        port => 65434
        codec => json {
            target => "[document]"
        }
    }
}

filter {
    mutate {
        remove_field => "user_agent"
    }
}


output {
    elasticsearch {
        hosts => ["https://elasticsearch:9200"]
        index => "hello"
        user => "elastic"
        password => "12345"
        ssl_verification_mode => none
    }
}